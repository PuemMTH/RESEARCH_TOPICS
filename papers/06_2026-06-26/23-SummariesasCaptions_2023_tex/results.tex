


\input{tables/result-summarization}

%In this section, we first describe the experimental setups and then show our automatic evaluation and human evaluation results.\kenneth{Opening needs a rewrite}

% \paragraph{Dataset.}
% \input{sections/data.tex}

%\subsection{Baselines}
\input{sections/baseline-models.tex}

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.48\linewidth]{figures/length-performance-rouge2-fmeasure.png}
    \includegraphics[width=0.48\linewidth]{figures/length-performance-rouge2-fmeasure-normalized.png}
    \vspace{-3mm}
    \caption{The relationship between average text length and ROUGE-2 score (left: original ROUGE-2; right: normalized ROUGE-2). The random baseline in the left figure shows that text lengths and scores are not independent. For example, when the predicted text is shorter than 50 tokens, predicting longer texts generally results in a higher ROUGE-2 score. The normalized scores indicate the proposed system's performance gain over the random baseline of the same length. Pegasus$_{P+O+B}$ and Reuse$_{M}$ get closer to TrOCR after normalization, suggesting the need for normalization for accurate interpretation of results.} 
    \label{fig:length-performance}
    \vspace{-3mm}
\end{figure*}

\paragraph{Experimental Setup.}
\label{sec:training-setup}
\input{sections/model-training-detail}


\subsection{Automatic Evaluation Results}
\paragraph{Conventional Automatic Evaluation.}
We used F1 of ROUGE-1, ROUGE-2, ROUGE-L~\cite{lin-2004-rouge,nallapati-etal-2016-abstractive}, 
MoverScore~\cite{zhao-etal-2019-moverscore}, and BERTScore for automatic evaluation. 
% ROUGE-1 F1 and ROUGE-2 F1 evaluated the F1 score of the unigram and bigram 
% token overlapping predicted texts and ground-truth captions. 
% ROUGE-L F1 computed the F1 score based on the longest common subsequence instead.
When computing ROUGE scores using rouge-score~\cite{rouge-scorer},
%\sukim{if the definition is clear and consistent, we don't have to cite this}
%\footnote{We used rouge-score: \url{https://github.com/google-research/google-research/tree/master/rouge}}
we turned all text into lower case and stem words.
As both MoverScore and BERTScore are based on the semantic similarity,
we obtained contextual embeddings from SciBERT~\cite{beltagy-etal-2019-scibert}.
%\footnote{We used \texttt{allenai/scibert\_scivocab\_uncased}.}

% MoverScore evaluated semantic similarity between predicted texts and ground-truth captions by 
% (\textit{i}) extracting contextual embeddings for words using BERT-based models and 
% (\textit{ii}) computing the Word Mover's Distance (WMD)~\cite{kusner2015word}, 
% where similar words are aligned and the cost for traveling between these words is accumulated. 
% In this work, we obtained contextual embeddings from SciBERT~\cite{beltagy-etal-2019-scibert}.\footnote{We used \texttt{allenai/scibert\_scivocab\_uncased}.}
% \cy{Add BERTScore}




% \begin{figure}
%     \centering
%     \includegraphics[width=0.9\linewidth]{figures/length-performance-rouge1 - fmeasure.png}
%     \vspace{-3mm}
%     \caption{ROUGE-1 scores with respect to length. Random shows within 50 tokens; longer texts lead to a higher score.}
%     \label{fig:rouge-1-length-performance}
%     \vspace{-2mm}
% \end{figure}



\paragraph{Automatic Evaluation with Normalization Over Caption Length.}
%ROUGE F1 has a limitation in that its scores favor longer texts within a certain length range, even though they take both precision and recall into account~\cite{sun-etal-2019-compare}, resulting in a misleading comparison, 
%as models that generate longer texts get higher scores. 
ROUGE F1 tends to favor longer texts within a certain length, leading to a skewed comparison where models generating longer texts receive higher scores~\cite{sun-etal-2019-compare}.
%Therefore, it is necessary to normalize automatic evaluation metrics with respect to the length of the text. 
We followed \citet{sun-etal-2019-compare}'s approach of normalizing the scores 
with the corresponding random baseline that generates texts of the same length. 
\begin{equation}
    \small
    Score_{normalized} = \frac{Score}{Random(length)}
\end{equation}
where $length$ is the average length of the texts generated by the target system.
We estimated $Random(length)$ by applying linear interpolation on several (length, random score) pairs.
The (length, random score) pairs were obtained by randomly selecting a certain number of sentences (1, 2, ..., 10 sentences) from the input paragraph as the prediction. 
To get random scores of texts shorter than a single sentence (around 30 tokens), 
we truncated sentences to the desired length (4, 6, ..., 30 tokens). 
For each length setting, we ran 10 different random seeds and reported the average. 
The Random line in \Cref{fig:length-performance}-Left shows the behavior of ROUGE-2
favoring longer texts within 50 tokens.\footnote{Similar trends for ROUGE-1, ROUGE-L, MoverScore, and BERTScore 
% can be seen in \Cref{fig:length-performance-rouge1} to \Cref{fig:length-performance-bertscore} 
are included in \Cref{sec:additional-results}.}
% \Cref{fig:length-performance-rouge1}, \Cref{fig:length-performance-rougel}, \Cref{fig:length-performance-wms}, \Cref{fig:length-performance-bertscore} in \Cref{sec:additional-results}).
% \footnote{ROUGE-1 shows the same behavior (see \Cref{sec:additional-results}).} 
The normalized scores, as shown in \Cref{fig:length-performance}-Right, 
%intuitively show how much better the proposed model is than the random baseline.
clearly indicate the superiority of our proposed model over the random baseline.


\Cref{tab:result-summarization} shows the normalized automatic evaluation results. 
Overall, \textbf{Pegasus$_{P+O}$, the text-summarization model with all available information (Paragraph+OCR), 
achieved the best performance in all three metrics.}
Pegasus$_{P+O+B}$, the model using the same information but trained on a better subset of captions (Paragraph+OCR-Better), did not perform well. 
%We hypothesized that this was because half of the testing data actually consisted of bad captions (see \Cref{sec:quality-annotation}). 
%We verified this by examining performance changes in beams of different quality (\Cref{sec:auto-eval-quality-beam}) and applying human evaluation to the ensuing paragraphs (\Cref{sec:human-eval}). 
We hypothesized this was due to half of the test data comprising poor captions (refer to \Cref{sec:quality-annotation}). 
This was validated by examining performance shifts in different quality beams (\Cref{sec:auto-eval-quality-beam}) and conducting a human evaluation (\Cref{sec:human-eval}).
Meanwhile, Reuse$_{M}$, the Reuse baseline with Mention, outperforms other Reuse baselines.
Its performance declined as context sizes grew and shifted.
%Reuse$_{M}$, the Reuse baseline with Mention, is the strongest Reuse baseline.
%The performance dropped when the context sizes increased and moved.
%implying that the surrounding contexts of Mention contained more relevant information. 
%We also found that OCR (Pegasus$_{O}$) was ineffective even compared with the vision-to-language models (TrOCR and BEiT+GPT2). 
%This may suggest that replacing pre-processed OCR texts with representations from a vision encoder would further improve performance.
%\kenneth{I removed the Pegasus$_{O}$ vs. OCR. Did we mention this later in the paper? If no, we can remove it. }

% \cy{say more about the intuitive of the normalization. How much better you are compared to the random baseline.}
% Interpolation
% 









% \begin{figure*}[t]
%     \centering
%     \includegraphics[width=0.98\linewidth]{figures/Quality-Beam-Normalized-Rouge1.png}
%     \caption{Normalized Rouge-1 for beams of different quality. \cy{We can probably move this figure to Appendix.}}
%     \label{fig:quality-beam-rouge-1}
% \end{figure*}








\subsection{Human Evaluation Results\label{sec:human-eval}}

%\kenneth{Add the small MTurk study we mentioned in the rebuttal.}

\paragraph{Pilot MTurk Study to Select Top Models.}
\cy{Probably emphasize why we choose these four systems for comparison}
Before the main human evaluation, we ran a pilot study on Amazon Mechanical Turk (MTurk) to identify any apparently underperforming baselines for exclusion in the final study, simplifying the main human evaluations.
In this study, we asked MTurk workers to carefully read a figure and select the \textit{worst} figure caption among 
{\em (i)} TrOCR, 
{\em (ii)} Pegasus$_{P+O}$, 
{\em (iii)} Pegasus$_{P+O+B}$, and 
{\em (iv)} ground-truth caption. 
%\sukim{The notation for the models above should be consistently used, including when you first introduce them at the beginning of section 4.2.}
Ninety figures without errors were randomly sampled from our annotated set (\ie, figures from \texttt{cs.CL} arXiv papers in \Cref{sec:quality-annotation}) for the study.
For each of the figures, we recruited 20 MTurk workers to judge.\footnote{Four MTurk qualifications were used: Locale (US Only), HIT Approval Rate ($\geq$98\%), Number of Approved HITs ($\geq$3000), and the Adult Content Qualification. The payment for each task was set to 0.09 (hourly wage = \$10 dollars).}
We report the number of majority votes (when tied, we counted all captions with the highest votes as the worst) and the average number of votes in \Cref{tab:mturk-eval}.
Results indicated that TrOCR's caption won the majority vote 41 out of 90 times, with its average vote count significantly exceeding others.
Hence, we excluded TrOCR from our formal human evaluation.


\paragraph{Main Human Evaluation with Domain Experts.}
%The human evaluation was conducted to assess the quality of the generated captions. 
Three Ph.D. students with NLP backgrounds (who are not coauthors) were recruited as human judges, 
as it is hard for those without basic domain understanding to evaluate captions.
This study has been approved by the IRB office of the authors' institute.
The same 90 figures used in the pilot MTurk study were used again.
% We randomly sampled 90 figures without errors from our annotated set (\ie, figures from \texttt{cs.CL} arXiv papers in \Cref{sec:quality-annotation}) 
We asked the human judges to compare each figure's 
(\textit{i}) Pegasus$_{P+O}$, (\textit{ii}) Pegasus$_{P+O+B}$, and (\textit{iii}) ground-truth caption. 
The judges were asked to rank the captions based on how strongly they agreed with this statement:
``When I read the paper, this caption can help me understand the message that the figure tries to convey.'' 
% Figure~\ref{fig:ui-compare} in \Cref{sec:app-interface} shows the interface and instructions for human judges.
\Cref{fig:ui-compare} (see \Cref{sec:app-interface}) shows the interface the human judges used. %where the initial order of the captions was randomized to avoid bias.
%The paper's title (without linking to the paper's URL) and abstract are shown. 
%The human judges can drag the captions (each displayed with the figure) 
%from the left pane into to the right pane to rank them. 
%The initial display order of the captions is randomized on the interface. 
%To prevent human judges from biasing toward the author-written captions, we did not display the paper's PDF or link to the paper's URL. 


\input{tables/human-mturk-eval.tex}

\input{tables/human-eval-score}

\begin{comment}

As shown in Figure~\ref{fig:ui-compare}, 
we instructed human judges to 
(\textit{i}) rank the caption as \textit{bad} if all information is obviously incorrect, and 
(\textit{ii}) ignore errors and focus on ``whether the caption can help understanding the figure'' if only a small part of the information is incorrect.\kenneth{TODO: UPDATE the REFs}

\end{comment}


\Cref{tab:human-eval} shows the results of average ranking (from 1 to 3). 
Overall, \textbf{the ground-truth caption and Pegasus$_{P+O+B}$ were ranked similarly} (1.919 vs. 1.930 with p-value = 0.923). 
\textbf{Humans also favored Pegasus$_{P+O+B}$ over Pegasus$_{P+O}$ significantly} (1.919 vs. 2.152 with p-value = 0.016).
This supports our heuristic for automatically determining caption quality based on length 
and aligns with previous findings that longer captions improve reader comprehension~\cite{hartley2003single,gelman2002let}. 
However, we found that the task of caption ranking poses a challenge, as evidenced by the lower correlations between raters, with Kendall's tau values of 0.133, 0.148, and 0.274, and Spearman's rho values of 0.128, 0.156, and 0.317.
This highlights the complexity of the task and suggests that scaling human evaluation across domains might be difficult.
Different preferences over captions, such as length, could lead to lower agreement among raters.

% However, we found that the caption ranking task is challenging as evidenced by the lower correlations between raters (Kendall's tau: 0.133, 0.148, and 0.274; and Spearman's rho: 0.128, 0.156, 0.317.)
% The values emphasize the complexity of the task and suggest that it might be difficult to scale the human evaluation to various domains.
% Another possible hypothesis is that people might have different preferences over captions, leading to low agreements.
% (short concise but correct information v.s. rich information but small number of errors).




%------------------- dead kitten -------------------

\begin{comment}

%------------ old draft below -----------

%------------ old draft below -----------




%\cy{A really rough draft. Need to rewrite.}\kenneth{Will take a look. Should probably add an UI screenshot to appendix.}
We conduct human evaluation to further examine the quality of the generated captions. Because figure captions vary greatly, it is difficult for those without basic domain understanding to evaluate them. We then recruit three Ph.D. students with NLP background to evaluate the quality.
We randomly sample 90 samples that has no errors from our annotated set (test set captions from {\tt cs.CL} papers). 
We compare Pegasus-Paragraph+OCR, Pegasus-Paragraph+OCR-Better, and ground truth caption. 
The task is designed to be a ranking task where annotators are asked to rank the captions to answer the question ``When I read the paper, this caption can help me understand the message that the figure tries to convey.''
In our pilot study, we notice that the generated captions sometimes contain factual errors.
Therefore, we instruct annotators to 
(\textit{i}) rank the caption as a bad caption if all the information is obviously incorrect;
(\textit{ii}) ignore the error and focus on ``whether the caption can help understanding the figure'' if only a small part of the information is wrong. \ed{maybe discuss more in error analysis}

\Cref{tab:human-eval} shows the results of average ranking (from one to three). 
Again, we provide the scores in beams of different quality to examine the behavior.
Overall, ground truth caption and Pegasus-Par.+OCR-BTR are ranked similarly (1.919 v.s 1.930) and both of them are significantly better than Pegasus-Par.+OCR (2.152) with p-values of 0.0145 and 0.0165, respectively. 
Considering beams of different quality, we find that Pegasus-Par.+OCR-BTR (1.867) is ranked better than ground truth captions (2.019) when they are not helpful. 

% We extracted CL papers 
% CL figure: 3559 / 419 / 438 (train/val/test)



Again, we presented the scores in beams of different quality to examine the behavior. 
Overall, the ground-truth caption and Pegasus$_{P+O+B}$ were ranked similarly (1.919 vs. 1.930 with p-value = 0.923)
and were both significantly better than Pegasus$_{P+O}$ (2.152), with p-values of 0.0145 and 0.0165, respectively. 
When considering beams of different quality, 
Pegasus$_{P+O+B}$ (1.867) was ranked better than ground-truth captions (2.019) when they were not helpful. 
On average, human judges preferred machine-generated captions 22 out of 35 times. 
%As a result, ZZ\% of the captions were considered not helpful to readers.
%We also found that the NLP researchers often preferred machine-generated captions, \ie, X out of Y times, for these cases where the original caption is unhelpful.
%Finally, we investigated the relations between the captions and the paragraphs that mention them.
The results showed an early evidence that, with some quality controls over training data, 
machines can summarize relevant paper contents to generate better captions and help readers.
However, due to the cost of recruiting experts to assess system outputs, 
more research is needed to perform a comprehensive evaluation of machine-generated captions on a larger scale.
%, especially when the human-written ones are bad.
%\kenneth{Maybe worth one more closing sentence.}
%\cy{done.}
%%% machine-generated captions is preferred
% 70 / 105 (all annotation)
% 22 / 35 (averaging)

\end{comment}


