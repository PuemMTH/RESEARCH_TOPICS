\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{figures/fig01/fig01_ablation_study-v5.png}
    %\caption{Result of ablation study on caption generation. Captions are the most important profile element, while images are more influential than paragraphs.
    %\kenneth{I like this design! (1) Enlarge font size. It's hard to read. (2) Can we pick a slightly darker color? These colors are fine, but a bit too light.}\sam{Updated with darker color and a bit larger font!}\kenneth{(1) What is the number on top of each bar? Maybe remove them. (2) Fonts need to be even bigger. (3) The label can be ``No Paragraph'', ``No Image''. ``No Caption'', (4) The bars can be slightly wider.}\sam{The number above is the average score for each setting. I think keeping them can make it more clear to know the actual difference? (Or create another table to show the full result of ablation study for clarity?)}\sam{P.S. I just make the chart flattened a bit, same setting as for EasyHard one}
    %}
    \vspace{-2pc}
    \caption{Ablation study on the impact of profile elements using GPT-4o. Results show a clear hierarchy of importance: caption > image > paragraph.}
    \vspace{-.5pc}
    \label{fig:ablation}
    %\vspace{-3mm}  % Reduce space
\end{figure}


%     \caption{Ablation study on the impact of profile elements. caption generation by removing one profile element at a time (caption, image, or figure-mentioning paragraph). Captions contribute the most, followed by images, then paragraphs.}